; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html
[env_default]
platform = espressif32 @ 6.5.0
board = esp32dev
framework = arduino
monitor_speed = 115200
; ✅ Allow bigger app size for BLE + WiFiProv
board_build.partitions = huge_app.csv

; ✅ Use the correct WiFiProv version (works with current core)
lib_deps = 
   espressif/WiFiProv @ ^2.0.4
; ✅ Enable more flash and PSRAM (if your board supports it)
board_build.flash_mode = dio
board_build.flash_size = 4MB
build_flags = 
    -DBOARD_HAS_PSRAM
    -mfix-esp32-psram-cache-issue
[env:Connect]
extends= env_default
build_src_filter=+<Connect>
; Optional: helps with BLE and WiFi provisioning examples

